let a = "paul"
let b = "Jean"

function checkName(firstName, secondName) {
    if (firstName === second Name) {
        return true 
    }
    else {
        return false
    }
}
 
let result = checkName(a, b)
console.log(result)

function calcul(number1, number2) {
    return number1 - number2
}

let resultCalcul = calcul(10, 40)

console.log(resultCalcul)